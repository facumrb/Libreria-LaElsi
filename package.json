{
  "scripts": {
    "start:dev": "npm-run-all --parallel start:dev:be start:dev:fe",
    "start:dev:be": "npm run start:dev --prefix ./be",
    "start:dev:fe": "npm start --prefix ./fe"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
